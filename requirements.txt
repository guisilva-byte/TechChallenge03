# Core deep learning
torch>=2.4.0
torchvision
torchaudio

# Unsloth — fast QLoRA fine-tuning (install via pip with extra index)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth

# HuggingFace ecosystem
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
accelerate>=0.33.0
trl<0.9.0

# Quantization
bitsandbytes>=0.44.0

# Attention optimization
xformers
triton
